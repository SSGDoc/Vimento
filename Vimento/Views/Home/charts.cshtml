<html>
<head>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">

        // Load Charts and the corechart package.
        google.charts.load('current', { 'packages': ['corechart'] });

        // Draw the pie chart for Sarah's pizza when Charts is loaded.
        google.charts.setOnLoadCallback(drawSarahChart);

        // Draw the pie chart for the Anthony's pizza when Charts is loaded.
        google.charts.setOnLoadCallback(drawAnthonyChart);

        // Callback that draws the pie chart for Sarah's pizza.
        function drawSarahChart() {

            // Create the data table for Sarah's pizza.
            var data = new google.visualization.DataTable();
            data.addColumn('string', 'BlaBla');
            data.addColumn('number', 'dontknow');
            data.addRows([
                ['Yousee', 2],
                ['3', 1],
                ['Telia', 2],
                ['Duka', 1],
                ['Huawei 5G', 3]
            ]);

            // Set options for Sarah's pie chart.
            var options = {
                title: 'Markeds andele',
                width: 400,
                height: 300,
                is3D: true
            };

            // Instantiate and draw the chart for Sarah's pizza.
            var chart = new google.visualization.PieChart(document.getElementById('Markeds_Andele'));
            chart.draw(data, options);
        }

        // Callback that draws the pie chart for Anthony's pizza.
        function drawAnthonyChart() {

            // Create the data table for Anthony's pizza.
            var data = new google.visualization.DataTable();
            data.addColumn('string', 'fjabbe');
            data.addColumn('number', 'Kunder');
            data.addRows([
                ['Yousee', 200000],
                ['3', 100000],
                ['Telia', 150000],
                ['Duka', 10000],
                ['Huawei 5G', 300000]
            ]);

            // Set options for Anthony's pie chart.
            var options = {
                title: 'Antal kunder',
                width: 400,
                height: 300
            };

            // Instantiate and draw the chart for Anthony's pizza.
            var chart = new google.visualization.BarChart(document.getElementById('Antal_Kunder'));
            chart.draw(data, options);
        }
    </script>
</head>
<body>
    <!--Table and divs that hold the pie charts-->
    <table class="columns">
        <tr>
            <td><div id="Markeds_Andele" style="border: 1px solid #ccc"></div></td>
            <td><div id="Antal_Kunder" style="border: 1px solid #ccc"></div></td>
        </tr>
    </table>
</body>
</html>